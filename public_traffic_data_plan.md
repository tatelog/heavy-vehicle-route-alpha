# 公的交通データ連携メモ

## 方針

このプロトタイプでは、有料の商用交通APIを前提にしない。国、自治体、公的機関、または準公的な道路交通情報提供主体が無償で公開しているデータを優先して利用する。

主な用途は、建設現場・大型車両の通行ルート検討を支援すること。Google Mapsのような完全なライブ渋滞ナビではなく、公的な交通量・規制情報と現場チームの共有情報を組み合わせて、候補ルートのリスクを見えるようにする。

## 優先データ

### 1. JARTIC/国土交通省 交通量API

- 種別: 常時観測交通量、トラフィックカウンター観測値
- 想定用途: ルート周辺の観測交通量から混雑傾向を推定する
- 費用: 無料利用可とされるが、利用規約への同意が必要
- 実装方針:
  - ルート候補のバウンディングボックスを作る
  - JARTIC WFS APIへ `GetFeature` で問い合わせる
  - 観測点がルート近傍にある場合、交通量の大きさを候補スコアに反映する
  - 遅延分数は直接値ではなく「混雑傾向」として扱う

確認してほしいこと:

- 交通量API利用規約への同意方法
- アプリ・業務利用での可否
- 出典表記のルール
- 取得頻度の制限
- データの保存・再配布可否

### 2. JARTIC 交通規制情報オープンデータ

- 種別: 交通規制、通行止め、工事、規制区間など
- 想定用途: 候補ルートに重ねて、通行不可・注意区間として強く反映する
- 実装方針:
  - 公開データの形式を確認する
  - GeoJSON等へ整形できる場合は `public_traffic_sources.json` のソースとして接続する
  - ルート候補との距離照合で、規制に近い区間を赤/オレンジ表示する

確認してほしいこと:

- データ取得方法がAPIかダウンロードか
- 更新頻度
- 利用規約
- 商用/業務利用の可否
- 出典表記

### 3. 国土交通省 道路情報提供システム

- 種別: 道路規制、道路気象、路面情報など
- 想定用途: 注意情報として表示する
- 注意: 閲覧サイト中心の可能性があるため、スクレイピング前提にはしない

確認してほしいこと:

- 再利用可能なデータ提供形態があるか
- APIまたはオープンデータが存在するか
- 利用条件

## 実装ステップ

1. `public_traffic_sources.json` でデータソースの利用可否を管理する
2. JARTIC交通量APIの規約確認後、`CONFIG.publicTraffic.enabled` と `jarticTrafficVolume.enabled` を有効化する
3. ルート候補の周辺BBOXから交通量観測点を取得する
4. 交通量が大きい観測点を「混雑傾向」として候補スコアに反映する
5. 交通規制オープンデータを取得・整形し、通行止め/工事/規制区間を候補ルートへ重ねる
6. チーム共有マーカーは現場の最新情報として、公的データより優先して表示する

## 現在のコード状態

- `index.html` に `CONFIG.publicTraffic` を追加済み
- `routeTrafficImpact()` から `publicTrafficImpact()` を呼ぶ枠を追加済み
- 現時点では公的データ連携は無効
- 既存の候補評価は、通行可否データとチーム共有マーカーを使う

## 参照

- JARTIC オープンデータ: https://www.jartic.or.jp/service/opendata/
- JARTIC/国土交通省 交通量APIガイダンス: https://www.jartic-open-traffic.org/
- xROAD 常時観測交通量: https://www.xroad.mlit.go.jp/database/233/
- 国土交通省 道路情報提供システム: https://www.road-info-prvs.mlit.go.jp/roadinfo/
